import { TabPlaceholderScreen } from '../../../components/TabPlaceholderScreen';

export default function RequesterScreen() {
  return (
    <TabPlaceholderScreen
      title="Ordering Coming Soon"
      icon="bag-handle-outline"
      description="We removed the old request flow for now. This tab will come back once the next ordering experience is ready."
    />
  );
}
